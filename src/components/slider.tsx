import * as React from 'react';

type Props = {
    images: string[];
};

export const Slider = ({images}: Props) => {
    return (
        <div>
            <h2>hey there! It's Slider!</h2>
        </div>
    );
};
